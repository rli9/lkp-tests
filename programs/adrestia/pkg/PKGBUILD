pkgname=adrestia
pkgver=git
pkgrel=1
pkgdesc="A benchmark to test scheduler load balancer."
arch=('i386' 'x86_64' 'aarch64')
license=('GPL')
source=("https://github.com/mfleming/adrestia.git")
md5sums=('SKIP')

. $LKP_SRC/lib/tests/pkgbuild.sh

prepare()
{
	prepare_benchmark_path
}

build()
{
	CC=clang CFLAGS="" make_src_pkg
}

package()
{
	pack_src_pkg_execs
}
