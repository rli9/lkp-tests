pkgname=redis
pkgver=git
pkgrel=0
arch=('i386' 'x86_64' 'aarch64')
url="https://redis.io"
license=('BSD')
depends=()
source=("https://github.com/redis/redis.git#branch=unstable")
sha256sums=('SKIP')

. $LKP_SRC/lib/tests/pkgbuild.sh

prepare()
{
	prepare_benchmark_path
}

build()
{
	make_src_pkg
}

package()
{
	make_install_src_pkg PREFIX=$benchmark_path
}
