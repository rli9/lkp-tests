pkgname=fsmark
pkgver=git
pkgrel=1
arch=('i386' 'x86_64' 'aarch64')
url="https://github.com/josefbacik/fs_mark.git"
license=('GPL')
source=("$pkgname"::"https://github.com/josefbacik/fs_mark.git")
md5sums=('SKIP')

. $LKP_SRC/lib/tests/pkgbuild.sh

prepare()
{
	prepare_benchmark_path
}

build()
{
	make_src_pkg -- fs_mark
}

package()
{
	pack_src_pkg_contents
}
