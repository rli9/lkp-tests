pkgname=sockperf
pkgver=git
pkgrel=1
url="https://github.com/Mellanox/sockperf"
arch=('x86_64' 'i386' 'aarch64')
license=('GPL')
source=("https://github.com/Mellanox/sockperf.git#branch=sockperf_v2")
md5sums=('SKIP')

. $LKP_SRC/lib/tests/pkgbuild.sh

prepare()
{
	prepare_benchmark_path
}

build()
{
	make_src_pkg
}

package()
{
	pack_src_pkg_contents
}
