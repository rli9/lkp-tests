pkgname=pybench
pkgver=1.1.3
pkgrel=1
url='https://svn.python.org/projects/python/trunk/Tools/pybench/'
arch=('i386' 'x86_64' 'riscv64' 'aarch64')
license=('custom')
source=('http://www.phoronix-test-suite.com/benchmark-files/pybench-2018-02-16.tar.gz')
md5sums=('e3ca0e887e4310896d32dd1cbd258a94')

. $LKP_SRC/lib/tests/pkgbuild.sh

# No compile step; override the default build() (make_src_pkg from pkgbuild.sh)
# so install doesn't run make on the unbuilt source.
build()
{
	:
}

prepare()
{
	prepare_benchmark_path
	rename_versioned_src_pkg_dir pybench-2018-02-16
}

