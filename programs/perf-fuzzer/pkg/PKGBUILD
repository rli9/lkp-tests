pkgname=perf-fuzzer
pkgver=git
pkgrel=1
url="https://github.com/deater/perf_event_tests"
arch=('x86_64' 'i386' 'riscv64' 'aarch64')
license=('GPL')
source=("$pkgname"::"https://github.com/deater/perf_event_tests.git")
md5sums=('SKIP')

. $LKP_SRC/lib/tests/pkgbuild.sh

prepare()
{
	prepare_benchmark_path
}

build()
{
	cd_src_pkg_dir

	make
}

package()
{
	pack_src_pkg_contents
}
