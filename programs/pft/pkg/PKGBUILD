pkgname=pft
pkgver=git
pkgrel=1
arch=('i386' 'x86_64' 'riscv64' 'aarch64')
url="https://github.com/gormanm/pft"
license=('GPL')
source=("https://github.com/gormanm/pft.git")
md5sums=('SKIP')

. $LKP_SRC/lib/tests/pkgbuild.sh

prepare()
{
	prepare_benchmark_path
}

build()
{
	cd_src_pkg_dir

	make pft
}

package()
{
	pack_src_pkg_contents pft
}
