pkgname=mce-log
pkgver=git
pkgrel=1
url="https://github.com/andikleen/mcelog"
arch=('x86_64' 'i386' 'aarch64')
license=('GPL')
source=("$pkgname"::"https://github.com/andikleen/mcelog.git" "https://git.kernel.org/pub/scm/utils/cpu/mce/mce-inject.git" "https://github.com/aegl/mce-test.git")
md5sums=('SKIP' 'SKIP' 'SKIP')

. $LKP_SRC/lib/tests/pkgbuild.sh

prepare()
{
	prepare_benchmark_path
}

build()
{
	make_src_pkg
	make_src_pkg -- -C ../mce-inject
	CPPFLAGS="" make_src_pkg -- -C ../mce-test
}

package()
{
	pack_src_pkg_contents

	make_install_src_pkg destdir="$pkgdir" -C ../mce-inject
	make_install_src_pkg -C ../mce-test

	DESTDIR=${pkgdir}/usr/bin pack_src_pkg_contents ../mce-test/bin/page-types
}
